#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < n; i++)


int main() {
  int N;
  cin >> N;

  int sx, sy, tx, ty;
  cin >> sx >> sy >> tx >> ty;

  vector<vector<int>> p(N, vector<int> (3));
  rep(i, N) cin >> p[i][0] >> p[i][1] >> p[i][2];


  rep(i, N) {
    s = p[i];
    while (true) {

    }
  }

}
